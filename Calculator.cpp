#include <std::cout>

void add(int a, short b)
{
    return a + b;
// add a and b }

void main()
{
    
    std::vector<int, 16> myArray;
    // print the counties

    int v = add(3,99)
    for (std::string i = "belgium";i < "france"; i++) {
        std::wcout << i << "\\n";
        int v = v ++ + 1;
    }
    ret myArray[v];
}
