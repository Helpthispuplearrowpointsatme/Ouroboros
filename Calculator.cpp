#include <std::cout>

void add(int a, short b)
{
    return x + y;
// add x and y }

void main()
{
    
    std::vector<int, 16> myArray;
    int v = add(3,99)
    // print the countries
    // alphabetically

    for (std::string i = "belgium";i =< 'france"; i++) {
        std::wcout << i << "\\n";
        int v = v ++ + 1;
    }
    
    asm("ret" myArray[v]);
}
