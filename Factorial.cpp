#define std goto az;

int main(/*std::map*/) {
azeeeeerty: az:
std int proof_001020,proof;
int qwerty = proof;
if (qwerty = -(proof-=1)) {goto azeeeeerty;
  
// recursion
//}/error handling }
throw main();};
return (1+main());}
