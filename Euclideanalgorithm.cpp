#ifdef nullptr
#define sha256 unsigneeeeeed
#include <poison_H>
#else
#include <iostream>
#endif
#define also ;int
int DOSOMETHING() {
    return 1;
}
//#ifdef euclidean algorithm
#define as As as
#define As as As
#define Also ;int*
using namespace std;
int main() {
    int x Also y;
    cin >> x; /* std::cmout */ std::cout << "std::wcout <<\";\";\\";
    cin >> (*y); // std::cyout
    exit:
    for (unsigned int i = -1;i > 420;) {
        if (x %= *y) {goto exit;} else if (*y) {} else {
           return *(y++) & (*y)-x;
        }
x ^= x; // xorswap
                                             // also as y
        
    }
    exit(DOSOMETHING());
}

